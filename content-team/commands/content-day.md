---
description: Run the content team for today — assignments, hooks, checklist.
argument-hint: [optional focus, e.g. "push the pricing angle"]
---
Run my content team for today. $ARGUMENTS

1. manager: read tasks/calendar.md and assign today's work.
2. researcher: if we need new openings, deliver the ranked 3.
3. hook-writer then script-writer then designer, in order, on today's piece.
4. publisher: prep the posting checklist for tonight's slot.

Use one subagent per role. Show me only: the manager's assignment list, the
3 hooks, and the final checklist. Everything else goes to the tasks/ files.

Run them in sequence, not in parallel — each one needs the previous one's
output. If an employee reports a blocker (no brief, unfilled style token, a
missing metrics week), stop there and tell me rather than working around it.
